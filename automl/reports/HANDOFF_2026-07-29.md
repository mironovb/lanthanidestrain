# Operational report — re-analysis campaign, 29 July 2026

Written to the format `AGENTS.md` requires: outcome, scope, validation, cluster
state, and the risks and assumptions that remain.

---

## Outcome

Nine measurements, **all complete**. Scientific content is in
[`REANALYSIS_2026-07-29.md`](REANALYSIS_2026-07-29.md); this document is the
operational record.

| campaign | state | result file |
|---|---|---|
| dual-key re-scoring | **complete** | `DUALKEY_RESULTS.md`, `dualkey_test.csv` |
| metric ceiling | **complete** | `ceiling_test.csv` |
| magnitude calibration | **complete** | `CALIBRATION_RESULTS.md`, `calibration_test_{binned,strict}.csv` |
| GPU determinism | **complete** | `DETERMINISM_RESULTS.md` |
| reference xTB energetics | **complete** | `ENERGY_RESULTS.md`, `energy_test.csv`, `energy_diagnostic.csv` |
| conformer pilot | **complete** | `CONFORMER_RESULTS.md`, `conformer_diagnostic.csv` |
| third encoder | **complete** | `ENCODER_RESULTS.md`, `encoder_test.csv`, `encoder_arms.csv` |
| decomposed objective | **complete** | `OBJECTIVE_RESULTS.md`, `objective_test.csv`, `objective_cells.csv` |

## Scope — what changed

**New modules.** `automl/topo/{dualkey,ceiling,calibration,encoder,objective,
determinism,full_stack}_test.py`, `automl/topo/dist_gnn.py`,
`automl/qc/{reference_xtb,energy_features,energy_diagnostic,mtd_ensemble,
conformer_diagnostic}.py`, `automl/figures_reanalysis.py`.

**Modified shared code**, all additive with defaults that reproduce prior
behaviour:

| file | change | default |
|---|---|---|
| `automl/topo/train.py` | `--deterministic`, `--level-weight`, `--block-key`, `--no-triangles`, `--arch dist` | all off / unset |
| `automl/topo/snn.py` | sorted-scatter reductions, `use_triangles` | off / True |
| `automl/topo/control_factorial.py` | `key_col=`, rejects the new flags in `_matches` | `composition_key` |
| `automl/topo/best_stack.py` | `key_col=` on `nested_stack`, `_score` | `composition_key` |
| `automl/qc/xtb_backend.py` | `keep_log=`, `method=` on `optimize` | False / `gfn2` |
| `automl/dataset.py` | `gE` blocks, three `baseline_2d_energy*` presets, conditional energy merge | merge only if the artifact exists |

`automl/evaluation.py` is **unchanged** — it already took the block array as an
argument.

**New SLURM scripts.** `analysis.sh`, `campaign_driver.sh`, `determinism.sh`,
`reference_xtb.sh`, `topo_encoder.sh`, `topo_objective.sh`.

**Protected data: untouched.** Nothing was written to `data/`, including
`xtb_reference_calculation_queue.csv`, whose `calculation_status` column stays as
shipped. All new artifacts are under `automl/artifacts/` and `automl/reports/`.

**Existing reports: append-only.** `SYNTHESIS.md` and `README.md` received two
errata each. `PI_REPORT.md` and the other guard-protected documents were not
touched.

## Validation

Final run, job 5284912 (an earlier pass at job 5278413 gave 145):

- **`pytest automl/tests` — 153 passed** (109 baseline + 44 new). New tests:
  `test_dualkey.py` (10), `test_determinism.py` (8), `test_decomposed_loss.py`
  (6), `test_encoders.py` (12), `test_report_consistency.py` (8).
- **`control_guard --verify` — 324 frozen artefacts byte-identical**, 6 reports
  content-preserved. Source changes recorded, as that mechanism intends.
- **`pi_split --verify` — MATCH**, hash `6070dc55ee5ff2d0…` unchanged.
- **Standing precondition holds**: published S0 re-ensembles to exactly
  **+0.2382**, `stack_test` reproduces +0.0351 and +0.0296.

Every new encoder passes permutation- and edge-direction-invariance tests, the
same properties `snn.py` asserts for the published one. The fast corrected
bootstrap is asserted against the literal copy-tagged statistic at 1e-9.

## Cluster state

**Submitted and complete**: 5278413 (verification), 5278275 (dual-key), 5278121
(ceiling), 5278341/2 (calibration), 5278050 (determinism), 5278069/74 (xTB
probe), 5278075 array 0-15 (xTB campaign, 953/956 geometries), 5278303 (energy),
5278467 array 0-3 (conformer pilot, 120/120), 5278631 (conformer diagnostic),
5278104/5278105 arrays 0-15 (encoder, 32/32), 5278721 (encoder test), 5278490
(full stack), 5278438 (stack_test restore).

**Complete**: `ln_obj` array, decomposed-objective sweep, **48/48**, placed by
`automl/slurm/campaign_driver.sh` under the 40-task submit cap; then 5280890
(objective test) and 5284912 (final verification).

**Nothing is running.** The queue is empty.

**Not submitted**: nothing outstanding. The conformer campaign that the pilot was
meant to justify is **deliberately not submitted** — the pilot says it would not
work.

**Cancelled deliberately**: 5278028 and 5278130 (superseded by a ~200× faster
bootstrap; their outputs were preserved and re-derived), 5278267 (pilot design
did not fit the compute budget), 5278723/5278724 (duplicate submissions — see
risks).

## Mistakes made, and their state

1. **The compute plan misread the cluster limits.** It assumed 40 concurrent GPU
   jobs from `MaxSubmitJobs`; `GrpTRES` caps this account at **one node** on
   `xeon-g6-volta`. A 648-cell factorial was planned and abandoned for 6 cells.
   *Corrected before any GPU time was spent.*
2. **The corrected bootstrap was written the slow way** and ran 75 minutes without
   finishing. *Fixed, ~200× faster, and asserted against the literal statistic.*
3. **A verification job overwrote `stack_test.csv` at `n_boot=100`.** *Restored
   byte-identical; `git status` clean.*
4. **`rm -rf` on my own incomplete pilot outputs** to force a clean re-run after a
   code change, where `--overwrite` was the disciplined route. Nothing protected
   was touched and the outputs were 20 minutes old, but `AGENTS.md` has a rule
   about deleting generated artifacts and this brushed it.
5. **A confound in my own sweep design** — the contrast objective batches only
   blocks with ≥2 rows, which would have given strict-key cells 67 % of the rows
   against binned cells' 96 %. *Caught while the array was queued; fixed for the
   decomposed objective and recorded as Amendment 1.*
6. **Three duplicate job submissions** caused by several monitors watching the
   same completion condition. *Two cancelled within a minute; no work lost.*

## Risks and assumptions that remain

- **The decomposed-objective result is in, and its one positive needs
  replication.** The arm adds +0.0150 [+0.0011, +0.0289] to the no-topology stack
  under the strict key after 19-look Bonferroni -- the only strict-key contrast in
  the re-analysis that survives correction. But it is a single confirmatory look on
  78 extractants, on a different row set from the dual-key test, and the direct
  OBJ-vs-S0 comparison on the same data is a null. Do not lean on it unreplicated.
- **The single-seed smoke read was misleading and nearly misled me.** At 5 folds x
  1 repeat x seed 7 the decomposed objective looked *ahead* of S0 (0.0397/0.0799 vs
  0.0217). At 8 seeds x 3 repeats it is behind. One seed is not evidence, and the
  interim read is recorded here as a caution rather than deleted.
- **A 4 % omission is left in place.** Every published topological arm, S0
  included, never sees its 202 singleton-block rows under the binned key.
  Discovered here, deliberately not corrected, because changing it would move
  published numbers. Recorded in `OBJECTIVE_PREREGISTRATION.md` Amendment 1.
- **The conformer pilot used GFN-FF geometries** with GFN2 energies. Adequate for
  its question; not adequate if the geometries were the deliverable.
- **The energy campaign covers 953 of 956 geometries.** Three produced no
  reference energies and are absent rather than imputed.
- **`--deterministic` was verified on one configuration.** It is not established
  that `--conformers`, `--block-centre` or `--arch picnn` reach bit-identity;
  those paths differ and were not checked.
- **The look count is now 19** for the topology claim (20 after `full_stack`).
  Every interval is reported uncorrected and Bonferroni-corrected side by side,
  but the honest reading of a claim that has been looked at twenty times is that
  the corrected column is the one that counts.

## Shortest safe next command

```bash
# when the ln_obj array reaches 48/48:
sbatch --partition=xeon-p8 automl/slurm/analysis.sh automl.topo.objective_test --n-boot 400
```

Then `python3 -m automl.figures_reanalysis --all` and
`python3 -m automl.topo.control_guard --verify`.

---

## Erratum, 30 July 2026 — the ceiling is WITHDRAWN

**Every statement in this document about a ceiling, about "39 % of attainable", or
about "+0.412 of headroom" is withdrawn.** See
[`AUDIT_2026-07-30.md`](AUDIT_2026-07-30.md).

The estimator measured how much the adjacent-pair difference moves across distinct
*exact* condition sets inside one binned block, and treated that as irreducible. It
is not: the model holds **64 exact numeric `cond__` columns** and 46 % of binned
blocks vary internally, and `adjacent_pair_arrays` averages **y and p on the same
grouping**, so per-condition variation never reaches the metric at all.

The contradiction that settles it: inside the 203-pair subset the estimate came
from, the implied ceiling is **+0.173** — *below* the best model's +0.267. The
+0.679 divided that subset's noise variance (0.0235) by the **full** 905-pair set's
variance (0.0733), populations 2.6× apart in spread.

`ceiling_v2.py` rebuilt it properly, joining the **DOI** column that
`raw_data/*_SAFE.csv` carries and the ML table dropped (100 % join, 110 DOIs). The
verdict is **not identifiable**:

- **94 %** of the disagreement between rows with identical model features is
  *within a single paper*, not between papers — so it is not source conflict, which
  was the assumption the first attempt rested on.
- A quarter of it is a recorded covariate the pipeline drops, resolving to one
  cause: `cond__diluent__other` collapses **42 distinct solvents** over 5.5 % of
  rows.
- Three quarters has **nothing recorded varying at all**, median SD 0.231 log
  units — which propagated to two single-measurement cells exceeds the entire
  observed spread of the target.

**The four closure results are unaffected** — recombination, energetics, conformers
and the objective each failed on its own terms. What is withdrawn is any claim
about *how much room* they were failing to reach, and consequently the framing of
them as "routes to the headroom". Optimisation targets must not be expressed as a
fraction of an attainable maximum, because that maximum is unknown.
